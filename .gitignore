node_modules
dist
.cursor
.env
.env.*
!.env.example
routing-rules.*.local.json
prisma/routing-rules.demo-walkthrough.local.json
prisma/routing-rules.production.local.json
.DS_Store
coverage
.turbo

# local editor / demo config (use ghl-demo-config.example.json for placeholders)
.vscode/
ghl-demo-config*.json
!ghl-demo-config.example.json

# accidental shell/env dump artifacts
*ntent*.env

# LeadCapture trust pilot local diagnostics (never commit)
.lc-trust-*
scripts/.lc-trust-*
