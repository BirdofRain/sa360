# Client onboarding walkthrough artifacts (2026-09-13)

Recorded against local Docker Postgres/Redis + admin-coc/API at commit `ee4963f` (same SHA as deployed API health).

## Video

`sa360-client-onboarding-walkthrough.webm`

On-screen banners mark CUSTOMER ACTION vs LAL INTERNAL ACTION. Chapter list: `chapters.json`.

This is **not** a production-data recording. Production was walked read-only (public pages + existing Smart Agent 360 Demo session). No production invites, orders, releases, or password changes were performed.

## Do not commit

`playwright-video/` raw chunks and any `walkthrough-error.*` files.
