# Customer journey final regression (post #96 / #98)

Validation-only rerun of the connected MVP customer journey against current
`origin/master` after:

- #96 customer release notification
- #98 PPL order-linked leads fix

This document is filled in after the harness and focused suites run. No product
features were added. No production deploy. Notification uses an injected test
transport only (no real customer email).
