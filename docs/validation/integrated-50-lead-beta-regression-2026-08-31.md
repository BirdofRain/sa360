# Integrated 50-lead beta regression (2026-08-31)

Validation-only proof that merged PRs **#105** (buyer-ready PPL reservation) and
**#106** (`buyer_csv_v4` customer spreadsheet) work together on the exact
50-lead Vet / aged-inventory scenario that previously forced a manual
spreadsheet cleanup plus a second order.

No product behavior was changed. No production deploy. No production writes.
Notification used an injected/fake transport only (`RESEND_API_KEY` unset).
All Prisma work targeted local `127.0.0.1:5432/sa360_test`.

## 1. Master SHA

`164fbbbc914a7dc73993f5c80b308f6a4e828d5b`

- `origin/master` at start: `164fbbb Add customer-ready PPL spreadsheet presentation (#106)`
- Branch: `validation/integrated-50-lead-beta-regression-2026-08-31`

## 2. Fixture composition

One deterministic tenant/order on the current customer lifecycle:

1. Portal-enabled Client A (`client_50beta_a_20260831`) and isolating Client B
2. Priced fulfillment-ops Client Lead Order: niche `vet`, state `NC`, quantity **50**,
   commerce bucket **`COMMERCE_3_6_MO`**
3. Payment confirm → approve (`ready`) → fulfillment-ops activate (`pay_per_lead` /
   `pooled_matching`)
4. Preview + commit selection for 50
5. Generate `buyer_csv_v4` package (unpublished)
6. Approve & Release via `markSpreadsheetDelivered`
7. Customer list/download + tenant B 404s + same-buyer reselection

Candidate inventory (66 rows, all NC / vet / aged / `COMMERCE_3_6_MO`):

| Class | Count | Insertion / scan |
| --- | --- | --- |
| Buyer-ready | **50** | Unique `generatedAt` (90–154 days), oldest-first insert |
| Rejected by #105 | **16** | Mixed through scan indexes `0,3,6,…,45` (not parked after the first 50) |

ZIP and Coverage Amount were blank on every row. Veteran niche fields
(Branch of Service, Disability Rating, Primary Concern, Beneficiary) were
populated on valid rows.

Invalid rows were interleaved in **oldest-first scan order**, so filling 50
required walking past rejected candidates rather than luckily taking a clean
prefix.

Valid `generatedAt` values were inserted oldest-first (not newest-first) so CSV
v4 sort could not inherit insertion or reservation order.

## 3. Invalid candidate counts / reasons

Selector `exclusionCounts.notBuyerReady` = **16**.

| Reason | Count |
| --- | --- |
| `missing_consumer_age` | 4 |
| `first_name_too_short` | 3 |
| `last_name_too_short` | 3 |
| `first_name_multipart` | 3 |
| `last_name_multipart` | 3 |

None of the 16 invalid item IDs appeared in reserved or exported allocations.
All 50 reserved payloads passed `isPplBuyerReadyLead`.

## 4. Quantity reconciliation

| Measure | Value |
| --- | --- |
| Requested (order + priced line) | 50 |
| Preview selected | 50 |
| Commit selected / allocation rows | 50 |
| `LeadOrder.reservedQuantity` after reserve | 50 |
| Overage (52 / 53) | none |
| Shortfall | 0 |
| Second order required to fill 50 | **no** |
| After release: committed allocations | 50 |
| Customer fulfillment presented | requested 50 / fulfilled 50 / remaining 0 / status `fulfilled` |
| `BuyerDeliveredIdentity` rows for those allocations | 50 |

## 5. Exact CSV schema

`fieldSchemaVersion` = **`buyer_csv_v4`**

50 allocations → **50 CSV data rows**.

Header (12 columns):

```
Date Generated, Lead Type, First Name, Last Name, Phone, Email, State, Age, Beneficiary, Branch of Service, Disability Rating, Primary Concern
```

| Check | Result |
| --- | --- |
| Column 1 | Date Generated |
| Column 2 | Lead Type |
| Lead Type every row | Veteran |
| ZIP | omitted (package-wide blank) |
| Coverage Amount | omitted (package-wide blank) |
| Age | populated on all 50 rows |
| Niche fields | Branch of Service / Disability Rating / Primary Concern retained |

## 6. First / last generated dates

CSV sort is `generatedAt` descending, independent of oldest-first insert and
oldest-first reservation:

| Position | Date Generated |
| --- | --- |
| First row (newest) | **2026-06-02** |
| Last row (oldest) | **2026-03-30** |

Confirmed `dates === [...dates].sort().reverse()`.

## 7. Release / download hash equality

| Artifact | SHA-256 |
| --- | --- |
| Generated (unreleased) package bytes | `cf7e2c06af02baf4e3788b5315e30864a12843e74d19a1bb9583c371797b5261` |
| Operator download after release | same |
| Customer secure download after release | same |

Before Approve & Release:

- operator package existed
- customer export list was empty
- customer download returned **404** `Delivery not found`

After release: customer list showed **exactly one** released package; download
`200 text/csv` with 50 data rows matching the generated bytes.

## 8. Order-linked lead reconciliation

`GET` customer order-linked leads (service + HTTP):

- **50** rows
- IDs = the same 50 committed allocation `sourceLeadEventId` values
- Tenant B cannot see them (404)

## 9. Notification proof

Injected/fake transport only. `RESEND_API_KEY` unset.

| Step | Result |
| --- | --- |
| Generate package | `customerReleaseNotifyStatus` = `null` (no send) |
| First Approve & Release | explicit pending intent → **1 send**, status `sent` |
| Replay same idempotency key | `already_sent`, send count stayed **1** |
| Later replay with failing injected transport | release still `ok`, **0 additional sends** |

Release succeeded independently of the fake transport (replay with injected
failure did not undo delivery).

## 10. Tenant-isolation proof

Tenant B (`client_50beta_b_20260831`) against Tenant A’s order/export:

| Action | Status |
| --- | --- |
| List package | **404** |
| Fetch package | **404** |
| Download package | **404** (`Delivery not found`) |
| Order-linked leads | **404** |

## 11. Same-buyer proof

After release, a clone of a delivered identity plus one fresh buyer-ready lead
were offered to a follow-on quantity-2 order for the same buyer:

- clone **not** selected (`sameBuyerPriorDelivery` ≥ 1)
- only the fresh identity was eligible

Released identities are not eligible for the same buyer again.

## 12. Tests run

| Check | Result |
| --- | --- |
| New integrated 50-lead harness | **1 passed / 0 failed** |
| Existing #105 buyer-ready unit tests | **10 passed / 0 failed** |
| Existing #105 eligibility policy tests | **7 passed / 0 failed** |
| Existing #106 `buyer_csv_v4` contract tests | **6 passed / 0 failed** |
| Combined focused run | **24 passed / 0 failed** |

Command (local `sa360_test` only):

```
SA360_TEST_DATABASE_URL=postgresql://sa360:***@127.0.0.1:5432/sa360_test
node --import tsx/esm --import ./src/test/set-test-env.ts --import ./src/test/prisma-test-teardown.ts --test --test-concurrency=1
  src/services/ppl-fulfillment/integrated-50-lead-beta-regression.integration.test.ts
  src/services/ppl-fulfillment/inventory-selection-buyer-ready.test.ts
  src/services/ppl-fulfillment/ppl-buyer-ready-eligibility.test.ts
  src/services/ppl-fulfillment/buyer-csv-v4.contract.test.ts
```

## 13. Files changed (test-only)

- `apps/api/src/services/ppl-fulfillment/integrated-50-lead-beta-regression.fixtures.ts`
- `apps/api/src/services/ppl-fulfillment/integrated-50-lead-beta-regression.integration.test.ts`
- `docs/validation/integrated-50-lead-beta-regression-2026-08-31.md` (this report)

No product, schema, route, or migration changes.

## 14. Risks / follow-ups

- Harness temporarily withdraws leftover NC/vet/aged `available` inventory in
  `sa360_test` and restores those IDs on cleanup. Other tests that re-seed
  (PPL beta fixtures) remain authoritative.
- Dates in this report are relative to the 2026-08-31 run (`generatedAt` is
  evaluated against `new Date()`).
- Fulfillment ops UX work can proceed; this does not activate NextGen
  `inventory_only`, PPL/LF2/GHL production flags, or live email.

## 15. Verdict

**PASS — COMBINED BETA DELIVERY CONTRACT PROVEN**
